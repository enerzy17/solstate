"""solstate - an auto-updating report on the state of the Solana ecosystem.

Zero dependencies (Python standard library only) and zero API keys for every
metric it publishes. See README.md.
"""
__version__ = "1.0.0"
